import 'package:flutter/material.dart';

// Class to display item widget
class ItemCard extends StatelessWidget {
  const ItemCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
